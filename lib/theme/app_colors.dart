import 'dart:ui';

class AppColors {
  AppColors._();

  // Backgrounds
  static const scaffoldBg = Color(0xFFF2F4F6);
  static const cardBg = Color(0xCCFFFFFF); // white at 80% for glass
  static const cardBorder = Color(0x14000000); // black at 8%

  // Text
  static const textPrimary = Color(0xFF191F28);
  static const textSecondary = Color(0xFF6B7684);

  // Accent
  static const primaryBlue = Color(0xFF3182F6);

  // Pros / Cons
  static const prosGreen = Color(0xFF34C759);
  static const consRed = Color(0xFFFF3B30);

  // Gradient
  static const gradientStart = Color(0xFF3182F6);
  static const gradientEnd = Color(0xFF8B5CF6);

  // Shimmer
  static const shimmerBase = Color(0xFFE5E8EB);
  static const shimmerHighlight = Color(0xFFF2F4F6);
}
